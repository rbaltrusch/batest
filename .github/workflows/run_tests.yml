name: Run Tests

# Controls when the workflow will run
on: [push]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2
      - name: Run tests
        working-directory: batest
        shell: cmd
        run: |
          batest . > __temp__
